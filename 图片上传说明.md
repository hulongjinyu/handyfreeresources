---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 7627858671879487795-data_volume/files/所有对话/主对话/用户上传/图片上传说明.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 2508457288154296#1785894745527
    ReservedCode2: ""
---
# 如何替换/上传网站图片

## 方法：把自己的图片传到 GitHub 仓库

### 第1步：准备图片
- 图片格式：JPG 或 PNG
- 建议尺寸：宽度 600px 以上，高度 400px 左右（横图效果最好）
- 文件命名：用英文或数字，不要用中文和空格，例如 `mountain01.jpg`、`sunset.jpg`
- 单张图片控制在 500KB 以内，网站加载更快

### 第2步：在仓库里创建 images 文件夹
1. 打开你的 GitHub 仓库 `hulongjinyu/handyfreeresources`
2. 点 **"Add file" → "Create new file"**
3. 在文件名输入框里输入：`images/.gitkeep`
   （这一步是为了创建 images 文件夹，.gitkeep 是个空文件，可以让空文件夹被 Git 识别）
4. 点页面底部的 **"Commit new file"**

### 第3步：上传图片到 images 文件夹
1. 点进刚才创建的 `images` 文件夹
2. 点 **"Add file" → "Upload files"**
3. 把你准备好的图片拖进去（可以一次拖多张）
4. 点 **"Commit changes"**

图片上传后，访问地址就是：
```
https://handyfreeresources.com/images/你的文件名.jpg
```

### 第4步：修改 index.html 里的图片链接
1. 回到仓库根目录，点开 `index.html`
2. 点右上角的 **铅笔图标**（Edit this file）
3. 找到图片链接，目前是这样的：
```html
<img src="https://picsum.photos/seed/scenic01/600/400" ...>
```
4. 替换成你上传的图片地址：
```html
<img src="images/mountain01.jpg" ...>
```
5. 每一张图对应一个 `scenicXX`，从 `scenic01` 到 `scenic50`，按顺序替换就行
6. 改完点页面底部的 **"Commit changes"**

### 第5步：查看效果
等 1-2 分钟，刷新 https://handyfreeresources.com 就能看到新图片了。

---

## 小技巧

### 批量替换图片链接
如果图片很多，不想一个一个改：
1. 把图片按 `scenic01.jpg`、`scenic02.jpg`...`scenic50.jpg` 命名
2. 全部上传到 images 文件夹
3. 在 index.html 里把所有 `https://picsum.photos/seed/` 替换成 `images/`，把 `/600/400` 删掉
4. 用 GitHub 网页的查找替换功能（编辑页面按 `Ctrl+F` 搜索）

### 图片建议
- 横图比竖图更适合画廊卡片展示
- 每张图配一个吸引人的英文标题和描述，有利于 Google 收录和 AdSense 审核
- 图片不要带水印、二维码或广告信息
- 确保你拥有图片版权或使用的是免费可商用图片（推荐 unsplash.com、pexels.com）

### 添加更多图片
现在网站有 50 张图，如果想加更多：
1. 在 index.html 里找到最后一张卡片（`scenic50` 那个）
2. 复制整段 `<article class="card">...</article>`
3. 粘贴一份，改标题、分类、描述和图片链接
4. 序号改成 51、52...
5. Commit 即可

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
